# revanced-patches (auto-synced mirror + dev builds)

This repository will be populated automatically from
https://gitlab.com/ReVanced/revanced-patches on the first scheduled/manual
run of the sync workflow. Until then, this placeholder README is here so the
repo isn't empty.

See [AUTOMATION.md](./AUTOMATION.md) for how the sync + build automation
works and the one-time setup step you need to do after pushing this repo.
